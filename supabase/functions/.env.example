# Copy this file to supabase/functions/.env and fill in real values.
# Read by `supabase functions serve` — local Edge Function secrets only.
# Never prefix with VITE_ — these must never ship to the client bundle.
# supabase/functions/.env is gitignored — never commit actual secrets.

TURNSTILE_SECRET_KEY=