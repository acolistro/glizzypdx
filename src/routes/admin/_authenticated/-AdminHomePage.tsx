// -----------------------------------------------------------------------
// WHAT THIS FILE DOES
// -----------------------------------------------------------------------
// The actual page content shown at /admin, once a visitor has passed the
// guard in admin/route.tsx. The leading `-` in the filename tells
// TanStack Router's file-based routing to ignore this file entirely --
// it's not a route, just a plain component, following the same
// convention already used by src/routes/-HomePage.tsx.
//
// Placeholder content for now: GLPDX-86/88/89/93 (the vendor approval
// flow) hasn't been built yet, so there's nothing real to show here yet.
// This will be replaced once that work lands.
// -----------------------------------------------------------------------

export function AdminHomePage() {
  return <h1>Admin</h1>;
}