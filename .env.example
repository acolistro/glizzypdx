# Copy this file to .env and fill in real values.
# .env is gitignored — never commit actual secrets.
#
# Two other env files exist for other purposes — see README > Environment variables:
#   supabase/functions/.env.example  → backend Edge Function secrets
#   .env.test.example                → E2E-only, local Supabase + dummy Turnstile keys

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_TURNSTILE_SITE_KEY=
VITE_STADIA_MAPS_API_KEY=
VITE_ANALYTICS_DOMAIN=